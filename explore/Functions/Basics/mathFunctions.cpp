#include <iostream>
#include<math.h>
using namespace std;
 
// maths functions has arguments type double and return type is also double.
// also need to include math.h

int main(){

    cout << pow(2, 4) << endl; // power x raised to power y
    cout << sqrt(16) <<endl; // square root

    return 0; 
}